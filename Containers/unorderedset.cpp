#include <bits/stdc++.h>
using namespace std;

int main() {
    unordered_set<int>st;
    //EVERYTHING IS SAME AS AN SET,ONLY DIFFERENCE IS THAT
    //IT IS NOT SORTED.
    //lower_bound and upper_bound() does not work here.

    return 0;
}