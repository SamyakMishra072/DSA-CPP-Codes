#include <bits/stdc++.h>
using namespace std;

int main() {
    deque<int>dq; //initialized a deque in stl in c++.
    dq.push_back(1);
    /* emplace_back, 
        push_front,emplace_front , pop_back;
        pop_front;
        dq.back();  //Accesses the last element of the deque.
        dq.front();   //dq.front(): Accesses the first element of the deque.

//rest all are similar just like vector in stl.
*/
    return 0;
}